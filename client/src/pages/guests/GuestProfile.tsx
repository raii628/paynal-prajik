// This will use the useParams to fetch the guest data

// For example: /profile/guest/:guest_id
// `:guest_id` will be the id of the guest that we want to fetch and display
const GuestProfile = () => {
  return (
    <div>GuestProfile</div>
  )
}

export default GuestProfile