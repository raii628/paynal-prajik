import Footer from "../layout/Footer";
import Hero from "../layout/Hero";

const Homepage = () => {
  return (
    <section>
      <Hero />
      <Footer />
    </section>
  );
};

export default Homepage;
